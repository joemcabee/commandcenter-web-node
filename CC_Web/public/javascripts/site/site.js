var baseUrl = document.location.origin + '/proxy/';
var baseUrlNoProxy = document.location.origin + '/';

function showLoader() {
    $('.fill').show();
}

function hideLoader() {
    $('.fill').hide();
}